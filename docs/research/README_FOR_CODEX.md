# The Dawning — Codex Handoff Bundle

This archive contains every file uploaded in the source conversation plus `UNIFIED_ANALYSIS_RESPONSE.md`, which contains the consolidated architectural assessment and recommended direction.

## Primary instruction

Treat **TheDawning V3 Layer 3+RT** as the canonical implementation. Treat older SpaceEngineD3D12/V1 archives as historical references and sources for selectively migrated algorithms or content. Treat V2 research documents as design and implementation references rather than a canonical executable codebase.

## Recommended first actions

1. Read `UNIFIED_ANALYSIS_RESPONSE.md`.
2. Read `CLAUDE(1).md`, `CONVERSATION_CONTEXT(1).md`, and `DC_HANDOFF(1).md`.
3. Inspect `TheDawningV3_Layer3RT_PathTracing(1).zip` as the canonical starting point.
4. Build a duplicate/archive manifest before extracting or merging older snapshots.
5. Do not blindly merge all ZIPs.
6. Establish a clean Git repository, CI, tests, and a canonical terminology map before adding new gameplay systems.
